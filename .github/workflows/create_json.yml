name: Create JSON

on:
  push:
   paths:
   - 'README.md'
  workflow_dispatch:


jobs:
  create-json:
    uses: thedim0n/.github/workflows/create_json.yml@main
    with:
      username: ${{ secrets.PAT }}
    
